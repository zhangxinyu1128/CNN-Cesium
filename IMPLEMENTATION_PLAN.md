# 项目复原与验收步骤

目标是让本地项目从源码启动，能用真实历史数据完成台风筛选、轨迹回放和 CNN 预测，并让每项预测结果可追溯到数据、模型、配置和测试记录。每个阶段只有通过验收后才进入下一阶段。无法从现有材料或数据证明的功能会标为未完成，不填入虚构指标。

## 已核对的依据

本计划对照 `baogao.docx` 和 23 页项目汇报 PPT 制定。当前数据审计结果为：年度索引覆盖 1945-2024 年，共 80 个 JSON；轨迹目录有 1,905 个文件，其中 1,903 个含有效台风对象、2 个为空；有效对象共 72,389 个观测点。相邻观测间隔并不固定为 6 小时，主要有 1、3、6 小时，也存在其他间隔。数据目录只有年度索引和最佳路径轨迹，没有风场、再分析格点、卫星云图或降水数据。

策划材料内部存在需要保留在验收记录中的差异：

- 摘要和总体设计提出融合再分析气象格点数据；第四章的实际网络说明则明确以纯轨迹序列为输入，第七章又把气象要素融合列为后续不足。当前没有格点数据，因此先实现并评估轨迹序列基线，气象融合在数据来源确认并接入后单独验收。
- 数据表描述 4 个历史步长、6 个输入特征；实验章节又称预测未来 1-6 步，而网络结构表只画出单步输出。基线实现采用 4 步输入、6 个特征，并直接输出未来 1-6 个 6 小时步长，避免把单步结果冒充多步结果。
- 系统目标提出不确定性概率锥体，但网络结构没有不确定性输出头或校准方法。后续须先留出独立校准集并验证区间覆盖率和宽度，达标后才允许绘制概率锥体；当前 API 将区间字段置为 `null`。
- 实验章节称数据覆盖 1949-2025 年、1,234 个案例，并给出 1,234 案例的划分和指标；本地数据实际为 1945-2024 年、1,903 条有效轨迹。经纬度误差以百分比表示、风速单位写为 `km/s`，这些结果不能当成本地复现指标。
- 实时气象接口、用户权限、数据库和管理员功能出现在概念架构中，但目前没有对应数据源或接口规范。核心离线演示先完成；实时数据和管理功能只有在明确来源、凭据和范围后才能验收。

## 分阶段实施与验收

| 阶段 | 工作 | 验收条件 | 状态 |
| --- | --- | --- | --- |
| 0. 对齐目标 | 锁定章节级模型、界面和数据要求，记录材料中的矛盾和缺失依赖 | 每项目标有实现位置、数据依赖和可执行验收项 | 本轮完成，存在的矛盾已登记 |
| 1. 环境与历史数据链路 | 恢复 Python 虚拟环境、FastAPI 数据 API、Vite 代理和历史台风页面 | 新环境按文档启动；年份、列表、详情接口与前端页面均可用 | 已实现并通过构建、类型检查和接口烟囱测试 |
| 2. 数据质量与可复现预处理 | 审计年度索引和轨迹，处理空文件、字段缺失、非法点、时间不规则；按台风划分数据并保存配置 | 生成 `data/processed/manifest.json` 和三个 JSONL；每个窗口形状为 `[4,6]` 输入与 `[6,3]` 标签；无跨集合台风、无非有限值、预测时间间隔严格 6 小时 | 已通过：33,761 个训练、926 个验证、1,143 个测试窗口 |
| 3. 基线和 CNN | 实现 Persistence 与匀速外推对照，再实现策划书中的两层 Conv1D、多任务输出、Adam、Dropout 和早停 | 固定随机种子；保存配置、日志、最优权重和测试指标；按 6-36 小时逐时效比较路径公里误差及风速 `m/s` 误差；未胜过基线的时效不得宣称提升 | 已完成训练和评估；路径 6/12/18 小时落后匀速基线，24/30/36 小时较好；风速 6 小时落后 Persistence、12-36 小时较好 |
| 4. 预测服务 | 服务启动时加载有效模型、归一化参数和版本；校验输入时间与坐标；输出训练定义的 1-6 个六小时时效 | `/api/predict` 与单样本离线推理输出一致；模型缺失时返回明确 503；无效时间步长和不支持的时效返回 422；自动化接口测试通过 | 已接通 checkpoint；HTTP 预测成功，单样本与离线推理最大差异 `1.9e-6`，异常输入返回 422；自动化测试已补 |
| 5. Cesium 预测交互 | 历史轨迹与预测轨迹分层显示；风速、强度、置信区间和预测时效联动 | 页面使用 API 实际结果；历史与预测分界清楚；模型、数据或接口异常有可见状态；桌面和窄屏无重叠 | 已接入预测窗口、API、时效/风速面板、地图预测层和离线底图；桌面基础视图通过浏览器检查，端到端预测交互与窄屏验收待完成 |
| 6. 可靠性与附加功能 | 完善登陆筛选、轨迹详情、风圈、图表、导出、服务健康状态；再评估多台风比较和资源图层 | 每个交互有可重复验收步骤；缺失风圈数据不伪造；导出包含单位和模型版本 | 部分历史功能已有，待逐项核查 |
| 7. 全链路验收 | 从干净环境启动前后端，运行 API、模型、前端、生产构建和性能测试 | 一键或按文档启动；验收记录含环境版本、数据清单、模型哈希、指标、截图和日志 | 待执行 |
| 8. 气象格点融合与实时数据 | 在获得可合法使用且覆盖对应台风的格点/实时数据后，接入环境分支并做固定测试集对照 | 数据来源、时间对齐、空间裁剪和许可可追溯；完成“去掉气象分支”消融，证明增益后才称为融合模型 | 阻塞：当前项目没有这类数据源或凭据 |

## 当前执行顺序

阶段 2 的数据审计、重采样和样本划分已完成并冻结。阶段 3 已完成 Persistence、匀速外推和 1D CNN 的固定测试集评估；CNN 只在部分较长时效胜过匀速基线，仍需改进短时效路径误差。阶段 4 已将真实 checkpoint 接入 API，并验证接口推理与离线输出一致。阶段 5 已接入前端预测请求和 Cesium 预测图层；尚须完成真实预测交互、移动视口和浏览器控制台的验收。概率锥体使用独立校准数据验证覆盖率，不把模型点预测包装成概率。多源气象分支及实时接口保留为阶段 8，直到数据源可用。

## 每阶段证据

阶段产物集中保存于 `data/processed/` 与 `artifacts/`：数据清单和拆分、配置、训练日志、模型权重及 SHA256、逐预测时效测试指标、消融结果、API 测试记录和页面验收截图。当前训练记录在 `artifacts/runs/20260923T132203Z/`，模型 SHA256 为 `136de0d4ad9441bcd92b0518fcd00a453823a742f1beb5db6d84d6e9040574e4`。报告中的旧样本数量、单位和性能数字不作为通过条件；完成实测后再决定是否修订报告。
